# Data Programming Individual Project

Libraries:

- `requests` for downloading datasets.

## Running

Enable venv and install deps.

```bash
source venv/bin/activate
pip install -r requirements.txt
```